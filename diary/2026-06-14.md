# 2026-06-14

## Session Summary

Review-only session — no code was changed.

Ran two `/adversarial-review` passes on the `xray_projection_render` repo on branch `cuda-backend-wired`:

**Pass 1: CUDA backend**
Focused on `cuda_backend.cu`, `cuda_backend.go`, `cuda_path.go`, `cuda_voxel.go`. Key findings:
- Texture coordinate axis permutation (`x`/`y` swapped in `tex3D` call) — silent wrong output for non-cubic volumes
- No CPU vs CUDA consistency test — completely undetectable scientific errors
- CUDA path silently ignores deformation files
- CUDA inaccessible from Python API (`api.go` hardcodes `use_cuda: false`)
- `ds` step size computed from NX only — aliasing for non-cubic volumes
- Voxelizer density clamping causes systematic attenuation underestimate vs direct CPU path

**Pass 2: CPU renderer, API, and code layout**
Focused on `main.go`, `api.go`, `objects/`, `deformations/`, `main_test.go`. Key findings:
- Hierarchical integrator double-counts density at every material boundary
- Both integrators miss the final partial integration interval (systematic underestimation)
- `VoxelGrid.Density` uses `[z][y][x]` layout; `ExportToRaw` writes `[z][x][y]` — round-trip swaps axes
- `log.Fatal().Err(err)` without `.Msg()` silently drops errors at 4+ call sites — process may continue with nil data
- `TessellatedObjColl` produces zero-density seams at unit-cell floating-point boundaries
- `AffineDeformation` replaces coordinates instead of displacing — inconsistent with all other deformation types
- `GreedyDensEval=true` makes negative-rho hole-carving impossible in tessellated collections
- `RenderProjections` is not thread-safe; `log.Fatal` in error paths kills the process, bypassing the recover()
- No API versioning; missing JSON fields silently zero-initialize
- `flat_field` has inconsistent units (integrated as optical depth, stored as pre-exponentiated in JSON)

**One false positive corrected by user:**
The pixel coordinate layout in `computePixel` looks transposed vs camera convention, but this is physically correct for transmission X-ray imaging. User confirmed with an asymmetric object test. Saved to persistent memory so future sessions don't re-flag it.

**Tests found:** 4 functions in `main_test.go` — all test string parsing and angle math. Zero physics, zero rendering, zero API coverage.

## What's Next
Fix the silent error-drop bug (`log.Fatal().Err` without `.Msg`) first — it's a correctness trap that hides every other error. Then address the VoxelGrid index mismatch and the hierarchical integrator double-count. Then write the sphere chord-length test as the first scientific ground-truth test.
